# Spotify's bundled Chromium cannot start its GPU process on this machine and
# aborts with "GPU process isn't usable. Goodbye." before drawing a window.
# Dropping the GPU sandbox lets it initialise; --disable-gpu and forcing ozone
# wayland both still failed, so this is the flag that actually matters.
--disable-gpu-sandbox
