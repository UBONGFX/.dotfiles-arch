# logs
*.log
*.qslog

# editor / OS cruft
*~
.DS_Store

# generated by wallust on theme switch (Theme falls back to Ariadne if it's missing)
colors.json

# per-theme last-used wallpaper, written by the theme/wallpaper scripts
wallpaper-state

# portable bundle build artifact (rebuild with packaging/package.sh)
quickshell-shell-*.zip
